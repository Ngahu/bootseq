[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "bootseq"
version = "0.1.1"
description = "Deterministic bootstrap task sequencing engine"
requires-python = ">=3.9"
dependencies = ["tqdm>=4.60.0"]
[project.scripts]
bootseq = "bootseq.cli:main"
